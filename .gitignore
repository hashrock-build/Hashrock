node_modules
dist

# secrets / local config (NEVER commit) — keep the example template
.env
.env.*
!.env.production.example
**/.treasury.json

# Source archives (already extracted into the repo)
*.zip
bundle/

# large local media (recordings / renders)
*.mp4
